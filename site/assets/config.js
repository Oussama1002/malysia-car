/**
 * Le seul fichier à modifier sur le serveur. Il ne contient rien de secret :
 * l'API publique n'expose que la flotte visible et le dépôt d'une demande.
 */
window.MALYSIA = {
  // Adresse de l'API DriveFlow, sans barre finale.
  apiBase: 'http://79.143.180.186:8080/api',

  // Coordonnées affichées sur le site.
  phone: '+212 6 00 00 00 00',
  whatsapp: '212600000000',
  email: 'contact@malysiacar.ma',
  address: 'Casablanca, Maroc',
};
